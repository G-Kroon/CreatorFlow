{
  "name": "Campaign",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Campaign name"
    },
    "brand_id": {
      "type": "string",
      "description": "Associated brand ID"
    },
    "brand_name": {
      "type": "string",
      "description": "Brand name for easy access"
    },
    "status": {
      "type": "string",
      "enum": [
        "planning",
        "active",
        "completed",
        "cancelled"
      ],
      "default": "planning"
    },
    "start_date": {
      "type": "string",
      "format": "date"
    },
    "end_date": {
      "type": "string",
      "format": "date"
    },
    "deliverables": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "description": {
            "type": "string"
          },
          "platform": {
            "type": "string"
          },
          "completed": {
            "type": "boolean"
          }
        }
      }
    },
    "payment_amount": {
      "type": "number",
      "description": "Campaign payment"
    },
    "payment_status": {
      "type": "string",
      "enum": [
        "pending",
        "partial",
        "paid"
      ],
      "default": "pending"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "title",
    "brand_name"
  ]
}
